<% include_id ||= false %>
<div class="card nested-fields mb-2">
  <div class="card-body">
    <div class="row align-items-center">
      <div class="col-1">
        <span class="handle">☰</span>
      </div>
      <div class="col-9">
        <%= f.association :person, label: false, include_blank: "Sélectionnez une personne", wrapper: false, required: true %>
      </div>
      <div class="col-2">
        <%= link_to_remove_association "🗑", f, class: 'btn btn-sm btn-danger' %>
      </div>
    </div>
  </div>
  <%= f.hidden_field :position, data: { 'sortable-input': '' } %>
  <%= f.hidden_field :id if include_id %>
</div>
